from lettuce import step

@step(u'Given I go to the new device page')
def given_i_go_to_the_new_device_page(step):
    assert False, 'This step must be implemented'

@step(u'When I submit the form device with valid values')
def when_i_submit_the_form_device_with_valid_values(step):
    assert False, 'This step must be implemented'

@step(u'Then I should see a success message')
def then_i_should_see_a_success_message(step):
    assert False, 'This step must be implemented'

@step(u'And I should see the device in the devices list')
def and_i_should_see_the_device_in_the_devices_list(step):
    assert False, 'This step must be implemented'
